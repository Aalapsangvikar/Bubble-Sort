#include <stdio.h>
int main()
{
int i,j,a[100],n=7,temp=0;
for(i=0;i<n;i++)
printf("Emter the number of elements in an array :");
scanf("%d",&a[i]);
}
for(i=0;I,n;i++)
{
for(j=0;j<n-1;j++)
{
if(a[j]>a[j+1])
temp=a[j];
a[j]=a[j+1];
a[j+1]=temp;
}
}
for(i=0;i<n;i++)
printf("%d",a[i]);
}
